# 上游合并记录（2026-09-20）

- 合并前：`0599df6`，备份分支 `codex/backup-before-upstream-20260920`。
- 上游：`555cute/r20-quantum-trader` 的 `main`，最终纳入 `6dd1d5c`（v8.1.1）。首次合并 v8.1.0 后，上游新增的两项提交也已纳入。
- 保留交易复盘、完整分析包、模型请求归档、流式响应看门狗、Qwen3 直接恢复、已收盘 K 线过滤、精确数量步长、OKX 挂单保留时长、权益口径熔断、调度重试及 Windows 兼容。
- 本地定制迁入上游拆分后的 `llm/`、`council/`、`brain/`、`trader/`、`routers/`。保留上游多交易所执行、保护与路由约束。
- 外所 K 线按周期结束时间判断收盘，并统一 Gate 秒级时间戳；图表仍可展示形成中的 K 线。
- 修复 Docker 兼容模块打包；`npm run typecheck` 现在显式检查 `tsconfig.app.json`。

## 验证范围与限制

最终重点回归 **259 项全部通过**，覆盖交易复盘、模型传输与恢复、投委会、K 线、归因、数量量化、挂单、调度、前端构建与翻译、模块自由变量和目录文档。前端应用类型检查与 Vite 生产构建均通过；Python 编译检查通过。

完整测试集并非全部通过。Windows 上存在上游已有的 POSIX 权限、`fork`、`fcntl`、外部命令、SQLite 文件清理及过期快照断言问题；已在独立的原始上游检出中复现相应失败。不能将重点回归通过解释为完整上游测试全部通过。

验证未运行实盘交易，也未构建 Docker 镜像。复盘中心的完整成交历史仍以 OKX 为主；Binance/Gate 保留上游台账同步，其执行事件按场所隔离。详见 [交易复盘说明](analysis.md)。

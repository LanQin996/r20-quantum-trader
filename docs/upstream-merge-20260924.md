# 上游合并记录（2026-09-24）

- 本地起点：`9f6781d`；备份分支：`codex/backup-before-upstream-20260924`。
- 先快进同步 `origin/main` 到 `41e5f9f`，纳入既有 CI 修复与收益曲线交互更新。
- 合并 `555cute/r20-quantum-trader` 的 `upstream/main`：`34a0d29`（v8.3.0），纳入 378 个上游提交。
- 解决 9 个文件的合并冲突。保留复盘归档、模型请求记录、已确认 K 线过滤、权益基数风控、精确数量步长、OKX 分批平仓成交与余仓核验、可配置挂单保留时长及 Windows 锁兼容。
- 接入上游跨所保护巡检、行情流观测、Prometheus 指标、通知字段和部署脚本；分批通知同时保留实际成交比例与上游的方向、费用、场所、部分平仓标志。
- Docker 保留本仓库 GHCR 镜像、非 root 用户、OKX CLI、兼容模块和单容器网关监督方式；补入 PYTHONPATH、时区、部署入口。入口不再用示例配置覆盖 env_file 注入值，`all` 模式交由后端监督网关，避免重复启动。
- 测试适配：去除两处重复基线还原，明确 OKX 挂单 TTL 差异；为币种、挂单、投委会和归因测试补充隔离夹具；加强分批通知字段及非整数比例的断言。

## 验证结果

- 280 项重点 Python 回归全部通过，无跳过。覆盖复盘、导出、模型传输、看门狗、K 线、归因、挂单、调度、分批止盈、周期流程、抽取契约和自由变量检查。
- 上述重点回归的离线守卫报告：外网请求、配置指纹变化、受保护文件写入尝试均为空。
- 604 项上游功能补充测试：600 项通过、1 项跳过、1 项失败、2 项错误。未通过项分别为 Windows 下 `/proc/nope` 并非只读路径、SQLite 临时数据库清理时文件占用、离线守卫禁止导入探针启动子进程。对应测试和 `market_stream.py`、`metrics.py` 与上游相同，未为消除平台失败改变其运行时逻辑。
- 前端类型检查、Vite 生产构建通过；前端测试 411 项中 385 项通过、26 项失败。对照合并前 `origin/main`，26 个失败测试名称完全一致，没有新增失败项；已有问题包括 Windows 路径匹配及定制页面的样式/无障碍静态约束。
- Python 源码 793 个文件语法检查通过；Docker 启动脚本通过 Bash 语法检查；Git 差异检查通过，无冲突标记。

完整 Python 离线套件和完整抽取套件尝试未获得最终汇总，不能视为全量测试通过。当前环境没有 Docker，未构建容器镜像，也未启动交易服务或执行实盘交易。合并仅提交到本地，未推送远程。
